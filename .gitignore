# Nix / direnv
.direnv/
.envrc.local
result
result-*

# Go
backend/dist/
backend/*.zip
backend/bootstrap
# Bare binaries left by `go build ./cmd/<name>` (recipes use `go run` so
# these only land if you build directly from `backend/`).
backend/admin
backend/api
backend/gen-gdpr
backend/retention
backend/seed
backend/seed-policy

# Node / SvelteKit
node_modules/
.svelte-kit/
frontend/build/
frontend/coverage/
frontend/.env
frontend/.env.*
!frontend/.env.example

# Pulumi
infra/.pulumi/
infra/.pulumi-local/
infra/Pulumi.*.yaml.bak
infra/infra
infra/bin/

# Editors / OS
.idea/
.vscode/
.DS_Store
*.swp
